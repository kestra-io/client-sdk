/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateExecutionLabelsParameter model module.
 * @module model/CreateExecutionLabelsParameter
  */
class CreateExecutionLabelsParameter {
    /**
     * Constructs a new <code>CreateExecutionLabelsParameter</code>.
     * @alias module:model/CreateExecutionLabelsParameter
     * @param {(module:model/Array|module:model/[String])} instance The actual instance to initialize CreateExecutionLabelsParameter.
     */
    constructor(instance = null) {
        if (instance === null) {
            this.actualInstance = null;
            return;
        }
        var match = 0;
        var errorMessages = [];
        try {
            // validate array data type
            if (!Array.isArray(instance)) {
                throw new Error("Invalid data type. Expecting array. Input: " + instance);
            }
            // validate array of string
            for (const item of instance) {
                if (!(typeof item === 'number' && item % 1 === 0)) {
                    throw new Error("Invalid array items. Must be string. Input: " + instance);
                }
            }
            this.actualInstance = instance;
            match++;
        } catch(err) {
            // json data failed to deserialize into [String]
            errorMessages.push("Failed to construct [String]: " + err)
        }

        try {
            // validate array data type
            if (!Array.isArray(instance)) {
                throw new Error("Invalid data type. Expecting array. Input: " + instance);
            }
            // validate array of string
            for (const item of instance) {
                if (!(typeof item === 'number' && item % 1 === 0)) {
                    throw new Error("Invalid array items. Must be string. Input: " + instance);
                }
            }
            this.actualInstance = instance;
            match++;
        } catch(err) {
            // json data failed to deserialize into [String]
            errorMessages.push("Failed to construct [String]: " + err)
        }

        if (match > 1) {
            throw new Error("Multiple matches found constructing `CreateExecutionLabelsParameter` with oneOf schemas Array, [String]. Input: " + JSON.stringify(instance));
        } else if (match === 0) {
            this.actualInstance = null; // clear the actual instance in case there are multiple matches
            throw new Error("No match found constructing `CreateExecutionLabelsParameter` with oneOf schemas Array, [String]. Details: " +
                            errorMessages.join(", "));
        } else { // only 1 match
            // the input is valid
        }
    }

    /**
     * Constructs a <code>CreateExecutionLabelsParameter</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateExecutionLabelsParameter} obj Optional instance to populate.
     * @return {module:model/CreateExecutionLabelsParameter} The populated <code>CreateExecutionLabelsParameter</code> instance.
     */
    static constructFromObject(data, obj) {
        return new CreateExecutionLabelsParameter(data);
    }

    /**
     * Gets the actual instance, which can be <code>Array</code>, <code>[String]</code>.
     * @return {(module:model/Array|module:model/[String])} The actual instance.
     */
    getActualInstance() {
        return this.actualInstance;
    }

    /**
     * Sets the actual instance, which can be <code>Array</code>, <code>[String]</code>.
     * @param {(module:model/Array|module:model/[String])} obj The actual instance.
     */
    setActualInstance(obj) {
       this.actualInstance = CreateExecutionLabelsParameter.constructFromObject(obj).getActualInstance();
    }

    /**
     * Returns the JSON representation of the actual instance.
     * @return {string}
     */
    toJSON = function(){
        return this.getActualInstance();
    }

    /**
     * Create an instance of CreateExecutionLabelsParameter from a JSON string.
     * @param {string} json_string JSON string.
     * @return {module:model/CreateExecutionLabelsParameter} An instance of CreateExecutionLabelsParameter.
     */
    static fromJSON = function(json_string){
        return CreateExecutionLabelsParameter.constructFromObject(JSON.parse(json_string));
    }
}


CreateExecutionLabelsParameter.OneOf = ["Array", "[String]"];

export default CreateExecutionLabelsParameter;

