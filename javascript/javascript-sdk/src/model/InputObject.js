/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DependsOn from './DependsOn';
import Type from './Type';

/**
  * @typedef {Object} IInputObject
  * @property {String} id
  * @property {Type} type
  * @property {String} description
  * @property {DependsOn} dependsOn
  * @property {Boolean} required
  * @property {Object} defaults
  * @property {Object} prefill - Optional UI hint for pre-filling the input. Cannot be used together with a default value.
  * @property {String} displayName
  */

/**
 * The InputObject model module.
 * @module model/InputObject
 * @type {IInputObject}
 */
class InputObject {
    /**
     * Constructs a new <code>InputObject</code>.
     * @alias module:model/InputObject
     * @param {String} id - 
     * @param {module:model/Type} type - 
     */
    constructor(id, type) { 
        
        InputObject.initialize(this, id, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, type) { 
        obj['id'] = id;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>InputObject</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InputObject} obj Optional instance to populate.
     * @return {module:model/InputObject} The populated <code>InputObject</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InputObject();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], Type);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('dependsOn')) {
                obj['dependsOn'] = ApiClient.convertToType(data['dependsOn'], DependsOn);
            }
            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('defaults')) {
                obj['defaults'] = ApiClient.convertToType(data['defaults'], Object);
            }
            if (data.hasOwnProperty('prefill')) {
                obj['prefill'] = ApiClient.convertToType(data['prefill'], Object);
            }
            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InputObject</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InputObject</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of InputObject.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // validate the optional field `dependsOn`
        if (data['dependsOn']) { // data not null
          DependsOn.validateJSON(data['dependsOn']);
        }
        // validate the optional field `defaults`
        if (data['defaults']) { // data not null
          Object.validateJSON(data['defaults']);
        }
        // validate the optional field `prefill`
        if (data['prefill']) { // data not null
          Object.validateJSON(data['prefill']);
        }
        // ensure the json data is a string
        if (data['displayName'] && !(typeof data['displayName'] === 'string' || data['displayName'] instanceof String)) {
            throw new Error("Expected the field `displayName` to be a primitive type in the JSON string but got " + data['displayName']);
        }

        return true;
    }


}

InputObject.RequiredProperties = ["id", "type"];

/**
 * @member {String} id
 */
InputObject.prototype['id'] = undefined;

/**
 * @member {module:model/Type} type
 */
InputObject.prototype['type'] = undefined;

/**
 * @member {String} description
 */
InputObject.prototype['description'] = undefined;

/**
 * @member {module:model/DependsOn} dependsOn
 */
InputObject.prototype['dependsOn'] = undefined;

/**
 * @member {Boolean} required
 */
InputObject.prototype['required'] = undefined;

/**
 * @member {Object} defaults
 */
InputObject.prototype['defaults'] = undefined;

/**
 * Optional UI hint for pre-filling the input. Cannot be used together with a default value.
 * @member {Object} prefill
 */
InputObject.prototype['prefill'] = undefined;

/**
 * @member {String} displayName
 */
InputObject.prototype['displayName'] = undefined;






export default InputObject;

